## 版本说明

- 这是首个公开 beta 版本，完成了 `LoadSim / loadsim` 的命名统一。
- 当前版本已经具备 `cpu`、`ram`、`combo` 三个核心命令。
- CPU 部分已经支持整机范围的自适应目标控制，并补充了控制周期、采样窗口、死区和步长等调优参数。
- RAM 部分已经支持块大小、控制周期和速率限制，便于做更平滑的内存占用场景。
- README 已补充真实命令输出、示例和调优说明，便于首次使用。
